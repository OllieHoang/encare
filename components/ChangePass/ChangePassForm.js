import { View, Text, SafeAreaView } from 'react-native';
import React from 'react';

const ChangePassForm = ({ navigation }) => {
    return (
        <SafeAreaView>
            <Text>ChangePassForm</Text>
        </SafeAreaView>
    );
};

export default ChangePassForm;
